import{describe,expect,it}from'vitest';import{annualSavings,entitlementAllowed,planPublishable,projectedMrr,subscriptionOperational,supportSlaStatus,usagePercent,usageStatus}from'../../apps/web/lib/commercial-engine';
const plan={id:'p',name:'Scale',code:'SCL',status:'active' as const,monthlyPrice:100,annualPrice:1000,entitlements:[{key:'users',label:'Users',limit:10,enabled:true},{key:'runs',label:'Runs',limit:null,enabled:true},{key:'x',label:'X',limit:1,enabled:true}]};
describe('commercial engine',()=>{it('enforces entitlements',()=>{expect(entitlementAllowed(plan.entitlements,'users',5)).toBe(true);expect(entitlementAllowed(plan.entitlements,'users',11)).toBe(false)});it('classifies usage',()=>{const m={key:'x',label:'x',used:90,limit:100,unit:'x'};expect(usagePercent(m)).toBe(90);expect(usageStatus(m)).toBe('warning')});it('gates subscriptions and plans',()=>{expect(subscriptionOperational({id:'s',workspace:'w',planCode:'SCL',status:'trial',billingCycle:'monthly',renewalDate:'x',seats:1,owner:'o'})).toBe(true);expect(planPublishable(plan)).toBe(true)});it('tracks SLA state',()=>expect(supportSlaStatus({id:'1',workspace:'w',subject:'x',severity:'high',status:'open',owner:'o',openedAt:'x',slaHours:8,ageHours:8})).toBe('breached'));it('calculates commercial metrics',()=>{expect(annualSavings(plan)).toBe(200);expect(projectedMrr([{id:'s',workspace:'w',planCode:'SCL',status:'active',billingCycle:'annual',renewalDate:'x',seats:1,owner:'o'}],[plan])).toBeCloseTo(83.333,2)})});
