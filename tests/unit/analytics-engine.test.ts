import {describe,expect,it} from 'vitest';import {buildMetricView,formatMetric,metricStatus,trend,variancePercent,weightedAttribution} from '../../apps/web/lib/analytics-engine';import type {KpiDefinition,KpiResult} from '../../apps/web/lib/analytics-types';
const def:KpiDefinition={id:'1',workspaceId:'w',key:'task_completion_rate',name:'Task completion',description:'',category:'execution',format:'percent',target:90,warningThreshold:80,ownerRole:'Manager',cadence:'weekly',active:true};
const result=(value:number,priorValue=80):KpiResult=>({id:'r',workspaceId:'w',kpiId:'1',periodStart:'2026-01-01',periodEnd:'2026-01-07',value,priorValue,target:90,calculatedAt:'2026-01-08',sourceStatus:'complete'});
describe('analytics engine',()=>{it('calculates KPI variance and trend',()=>{const view=buildMetricView(def,result(85));expect(view.variance).toBe(-5);expect(view.variancePercent).toBe(-5.6);expect(view.trend).toBe('up');expect(view.status).toBe('watch')});it('marks higher-is-better metrics off track',()=>expect(metricStatus(def,result(70))).toBe('off_track'));it('handles lower-is-better metrics',()=>{const cycle={...def,key:'average_sales_cycle_days',format:'days' as const,target:45,warningThreshold:60};expect(metricStatus(cycle,{...result(52),target:45})).toBe('watch');expect(metricStatus(cycle,{...result(42),target:45})).toBe('on_track')});it('calculates weighted attribution and rejects invalid weight',()=>{expect(weightedAttribution(100000,.25)).toBe(25000);expect(()=>weightedAttribution(100,1.2)).toThrow()});it('formats governed metric types',()=>{expect(formatMetric(12500,'currency')).toBe('$12,500');expect(formatMetric(86.2,'percent')).toBe('86.2%');expect(trend(5,5)).toBe('flat');expect(variancePercent(1,0)).toBeNull()})});
