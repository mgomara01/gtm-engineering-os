import{describe,expect,it}from'vitest';import{anomalySeverity,closedLoopReadiness,forecastAccuracy,forecastTotal,outcomeLift,portfolioBenchmark,recommendationPriority}from'../../apps/web/lib/intelligence-engine';import type{ForecastModel,OptimizationRecommendation}from'../../apps/web/lib/intelligence-types';
const model:ForecastModel={id:'f',name:'x',metric:'m',workspace:'w',status:'active',horizon:'90',confidence:80,version:1,lastRun:'x',points:[{period:'1',predicted:90,lower:80,upper:100,actual:100},{period:'2',predicted:200,lower:180,upper:220}]};
const rec:OptimizationRecommendation={id:'r',workspace:'w',type:'funnel',title:'x',summary:'x',impact:80,confidence:90,status:'implemented',owner:'o',evidence:[],createdAt:'2026-01-01'};
describe('enterprise intelligence engine',()=>{it('totals forecasts',()=>expect(forecastTotal(model)).toBe(290));it('calculates measured forecast accuracy',()=>expect(forecastAccuracy(model)).toBe(90));it('calculates recommendation lift',()=>expect(outcomeLift({id:'o',recommendationId:'r',baseline:10,expected:15,actual:12,unit:'%'})).toBe(20));it('classifies anomalies',()=>{expect(anomalySeverity(130,100,10)).toBe('critical');expect(anomalySeverity(111,100,10)).toBe('warning');expect(anomalySeverity(105,100,10)).toBe('normal')});it('evaluates closed loop readiness',()=>expect(closedLoopReadiness([rec],[{id:'o',recommendationId:'r',baseline:10,expected:12,actual:13,unit:'%'}]).ready).toBe(true));it('compares portfolio benchmarks',()=>expect(portfolioBenchmark({id:'b',name:'n',workspaceValues:{w:12},portfolioMedian:10,unit:'number'},'w')).toEqual({value:12,variance:2,above:true}));it('ranks recommendation priority deterministically',()=>expect(recommendationPriority(rec)).toBe(80));});
