import {describe,it,expect} from 'vitest';import {weightedPipeline,pipelineValue,isOverdue,canAdvanceOpportunity,nextStage} from '../../apps/web/lib/execution-engine';import type {Opportunity,WorkItem} from '../../apps/web/lib/execution-types';
const opp=(stage:Opportunity['stage']='qualified'):Opportunity=>({id:'o',workspaceId:'w',name:'n',accountId:'a',accountName:'A',offerName:'x',stage,value:1000,probability:40,owner:'u',nextAction:'call',source:'manual',createdAt:'',updatedAt:''});
const task=(status:WorkItem['status']='ready',priority:WorkItem['priority']='high'):WorkItem=>({id:'t',workspaceId:'w',title:'t',description:'',status,priority,owner:'u',opportunityId:'o',dueAt:'2026-01-01T00:00:00Z',createdAt:''});
describe('execution engine',()=>{it('calculates pipeline values',()=>{expect(pipelineValue([opp(),{...opp('won'),id:'2'}])).toBe(1000);expect(weightedPipeline([opp()])).toBe(400)});it('detects overdue open work',()=>{expect(isOverdue(task(),new Date('2026-02-01'))).toBe(true);expect(isOverdue(task('completed'),new Date('2026-02-01'))).toBe(false)});it('blocks advancement for critical unresolved work',()=>{expect(canAdvanceOpportunity(opp(),[task('blocked','critical')])).toBe(false);expect(canAdvanceOpportunity(opp(),[task()])).toBe(true)});it('returns governed next stage',()=>{expect(nextStage('proposal')).toBe('negotiation');expect(nextStage('won')).toBeNull()})});
