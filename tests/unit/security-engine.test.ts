import { describe, expect, it } from 'vitest';
import { controlCoveragePercent, controlTestOverdue, dataAssetRisk, findingOverdue, findingRiskScore, privacyDaysRemaining, privacyRequestOverdue, remediationTargetDays } from '../../apps/web/lib/security-engine';
import type { DataAsset, PrivacyRequest, SecurityControl, SecurityFinding } from '../../apps/web/lib/security-types';
const finding:SecurityFinding={id:'f',title:'x',severity:'high',status:'open',asset:'api',owner:'ops',discoveredAt:'2026-07-01',dueAt:'2026-07-10',exploitAvailable:true,internetExposed:true};
const asset:DataAsset={id:'a',name:'Customers',system:'db',classification:'restricted',owner:'data',personalData:true,retentionDays:3000,encryptionAtRest:true,encryptionInTransit:true};
const request:PrivacyRequest={id:'p',type:'access',status:'in_progress',jurisdiction:'CA',receivedAt:'2026-06-01',dueAt:'2026-07-20',verifiedAt:'2026-06-02',owner:'privacy'};
const controls:SecurityControl[]=[{id:'1',framework:'SOC2',code:'x',title:'x',owner:'o',status:'effective',lastTestedAt:null,nextTestAt:'2026-08-01',evidenceCount:1},{id:'2',framework:'SOC2',code:'y',title:'y',owner:'o',status:'partial',lastTestedAt:null,nextTestAt:'2026-07-01',evidenceCount:1}];
describe('security engine',()=>{it('maps remediation targets',()=>{expect(remediationTargetDays('critical')).toBe(2);expect(remediationTargetDays('low')).toBe(90)});it('detects overdue findings',()=>expect(findingOverdue(finding,new Date('2026-07-18'))).toBe(true));it('scores exploit and exposure',()=>expect(findingRiskScore(finding)).toBe(80));it('calculates weighted control coverage',()=>expect(controlCoveragePercent(controls)).toBe(75));it('detects overdue control tests',()=>expect(controlTestOverdue(controls[1],new Date('2026-07-18'))).toBe(true));it('scores restricted personal data',()=>expect(dataAssetRisk(asset)).toBe(75));it('detects overdue privacy requests',()=>expect(privacyRequestOverdue(request,new Date('2026-07-21'))).toBe(true));it('calculates privacy deadline days',()=>expect(privacyDaysRemaining(request,new Date('2026-07-18'))).toBe(2));});
