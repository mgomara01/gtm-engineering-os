import {describe,expect,it} from 'vitest';import {calculateScore,priorityTier,validateWeights} from '../../apps/web/lib/scoring-engine';import type {ScoringModel} from '../../apps/web/lib/scoring-types';
const model:ScoringModel={id:'m',workspaceId:'w',name:'Test',description:'',status:'active',version:1,maxScore:100,priorityThresholds:{a:85,b:70,c:0},updatedAt:'',factors:[{id:'fit',name:'Fit',description:'',kind:'calculated',weight:60,enabled:true},{id:'value',name:'Value',description:'',kind:'ai_assisted',weight:40,enabled:true},{id:'exclude',name:'Exclude',description:'',kind:'calculated',weight:0,enabled:true,hardExclusion:true}]};
describe('scoring engine',()=>{it('validates active weights',()=>expect(validateWeights(model.factors)).toEqual({total:100,valid:true}));it('calculates weighted score and tier',()=>{const r=calculateScore(model,{factors:{fit:90,value:80}});expect(r.score).toBe(86);expect(r.tier).toBe('A');expect(r.results).toHaveLength(2)});it('honors hard exclusions',()=>{const r=calculateScore(model,{factors:{fit:100,value:100},excluded:true,exclusionReason:'Outside territory'});expect(r.tier).toBe('Excluded');expect(r.score).toBe(0)});it('classifies thresholds',()=>{expect(priorityTier(85,model)).toBe('A');expect(priorityTier(70,model)).toBe('B');expect(priorityTier(69,model)).toBe('C')});it('rejects invalid weights',()=>{const bad={...model,factors:model.factors.map(f=>f.id==='fit'?{...f,weight:50}:f)};expect(()=>calculateScore(bad,{factors:{fit:80,value:80}})).toThrow(/total 100/)})});
