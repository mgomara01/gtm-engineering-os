import {describe,expect,it} from 'vitest';import {canRetry,connectionHealth,reconciliationRate,retryDelaySeconds,syncSuccessRate} from '../../apps/web/lib/integration-engine';import type {IntegrationConnection,SyncJob} from '../../apps/web/lib/integration-types';
const connection:IntegrationConnection={id:'c',workspaceId:'w',name:'Test',provider:'Test',category:'CRM',direction:'inbound',status:'healthy',authType:'oauth2',lastCheckedAt:'2026-07-18T19:00:00Z',lastSuccessfulSyncAt:'2026-07-18T18:45:00Z',failureCount:0,recordsLast24h:10,ownerRole:'Admin',scopes:[]};
const jobs:SyncJob[]=[{id:'1',workspaceId:'w',connectionId:'c',connectionName:'Test',objectType:'a',direction:'inbound',status:'succeeded',startedAt:'x',recordsRead:10,recordsWritten:10,recordsRejected:0,retryCount:0},{id:'2',workspaceId:'w',connectionId:'c',connectionName:'Test',objectType:'a',direction:'inbound',status:'partial',startedAt:'x',recordsRead:10,recordsWritten:8,recordsRejected:2,retryCount:0}];
describe('integration engine',()=>{it('calculates weighted sync success',()=>expect(syncSuccessRate(jobs)).toBe(75));it('calculates connector health',()=>expect(connectionHealth(connection,jobs,new Date('2026-07-18T19:00:00Z')).status).toBe('degraded'));it('uses exponential retry delays',()=>expect(retryDelaySeconds(3)).toBe(240));it('limits retries',()=>expect(canRetry({...jobs[0],status:'failed',retryCount:5})).toBe(false));it('calculates reconciliation rate',()=>expect(reconciliationRate(100,5,3)).toBe(92))});
