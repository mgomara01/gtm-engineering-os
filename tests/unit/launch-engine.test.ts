import {describe,expect,it} from 'vitest';import {deploymentAllowed,environmentReadiness,launchReadiness,recoveryReadiness} from '../../apps/web/lib/launch-engine';
const gates=[{id:'1',category:'testing' as const,name:'Tests',status:'pass' as const,blocking:true,evidence:'ok',owner:'Eng'}];
const checks=[{name:'KEY',required:true,configured:true,scope:'server' as const,description:'key'}];
const recovery=[{id:'r',name:'DB',rpoHours:1,rtoHours:2,lastTested:'2026-01-01',status:'pass' as const,evidence:'ok'}];
describe('launch engine',()=>{it('calculates a passing launch score',()=>expect(launchReadiness(gates)).toMatchObject({ready:true,score:100}));it('detects missing environment configuration',()=>expect(environmentReadiness([{...checks[0],configured:false}]).ready).toBe(false));it('requires tested recovery controls',()=>expect(recoveryReadiness([{...recovery[0],lastTested:undefined}]).ready).toBe(false));it('allows deployment only when every control plane passes',()=>expect(deploymentAllowed(gates,checks,recovery)).toBe(true));it('blocks deployment on a blocking gate failure',()=>expect(deploymentAllowed([{...gates[0],status:'fail'}],checks,recovery)).toBe(false));});
