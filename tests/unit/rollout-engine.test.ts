import{describe,expect,it}from'vitest';import{adoptionStatus,cohortProgress,hypercareSummary,migrationSummary,stabilizationDecision,trainingSummary}from'../../apps/web/lib/rollout-engine';
describe('rollout engine',()=>{it('calculates cohort certification',()=>expect(cohortProgress({id:'1',name:'x',workspace:'w',users:8,certified:6,accounts:1,status:'live',owner:'o',targetDate:'x'})).toEqual({certificationRate:75,live:true}));it('classifies adoption directionally',()=>{expect(adoptionStatus({id:'1',name:'x',actual:90,target:85,unit:'percent',direction:'higher'})).toBe('on_track');expect(adoptionStatus({id:'2',name:'y',actual:3,target:2,unit:'days',direction:'lower'})).toBe('off_track')});it('blocks migration exceptions',()=>expect(migrationSummary([{id:'1',name:'x',sourceCount:10,targetCount:9,exceptions:1,status:'running',blocking:true}]).ready).toBe(false));it('requires certification',()=>expect(trainingSummary([{id:'1',name:'x',assigned:2,completed:1,passingScore:80,averageScore:90,blocking:true}]).ready).toBe(false));it('flags high hypercare issues',()=>expect(hypercareSummary([{id:'1',title:'x',severity:'high',status:'open',owner:'o',ageHours:1}]).ready).toBe(false));it('requires all graduation controls',()=>expect(stabilizationDecision([{id:'1',name:'g',status:'pass',blocking:true,evidence:'e'}],[{id:'1',name:'m',sourceCount:1,targetCount:1,exceptions:0,status:'reconciled',blocking:true}],[{id:'1',name:'t',assigned:1,completed:1,passingScore:80,averageScore:90,blocking:true}],[])).toBe(true))});
