import{describe,expect,it}from'vitest';import{canActivateWorkspace,canPublishTemplate,diffRiskSummary,packageFingerprint,portabilityStatus,provisioningProgress,templateCompleteness}from'../../apps/web/lib/extensibility-engine';
const template={id:'t',name:'Home Services',industry:'x',version:1,status:'draft' as const,description:'x',modules:['a','b','c','d','e','f'],requiredInputs:['a','b','c','d'],updatedAt:'x'};
describe('extensibility engine',()=>{it('scores and gates templates',()=>{expect(templateCompleteness(template)).toBe(68);expect(canPublishTemplate(template)).toBe(true)});it('measures provisioning and activation',()=>{const r={id:'r',workspaceName:'x',templateId:'t',owner:'o',status:'ready' as const,requestedAt:'x',checks:[{name:'a',passed:true,detail:''},{name:'b',passed:false,detail:''}]};expect(provisioningProgress(r)).toBe(50);expect(canActivateWorkspace(r)).toBe(false)});it('blocks incompatible packages',()=>expect(portabilityStatus({id:'p',packageId:'x',targetWorkspace:'y',schemaCompatible:false,conflicts:0,warnings:0,status:'pass'})).toBe('blocked'));it('creates stable fingerprints',()=>expect(packageFingerprint({name:'Home Services',version:2,sections:['b','a'],compatibleSchema:'24.0'})).toBe('home-services:v2:24.0:a,b'));it('summarizes diff risk',()=>expect(diffRiskSummary([{section:'x',change:'added',summary:'x',risk:'high'},{section:'y',change:'changed',summary:'y',risk:'low'}])).toEqual({high:1,medium:0,low:1}))});
