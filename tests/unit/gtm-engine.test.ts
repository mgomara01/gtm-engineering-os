import {describe,expect,it} from 'vitest';import {conversionRate,eligibleForPlaybook,enrollAccounts,nextStep,validatePlaybook} from '../../apps/web/lib/gtm-engine';import type {Playbook} from '../../apps/web/lib/gtm-types';
const playbook:Playbook={id:'p',workspaceId:'w',name:'Test',description:'',status:'active',version:1,offerId:'o',targetTier:'A',trigger:'signal',updatedAt:'',steps:[{id:'1',order:1,name:'Research',channel:'task',delayDays:0,ownerRole:'analyst',template:'',completionCriteria:'done'},{id:'2',order:2,name:'Email',channel:'email',delayDays:2,ownerRole:'seller',template:'',completionCriteria:'sent'}]};
describe('GTM execution engine',()=>{it('validates a complete playbook',()=>expect(validatePlaybook(playbook).valid).toBe(true));it('rejects duplicate step orders',()=>expect(validatePlaybook({...playbook,steps:[...playbook.steps,{...playbook.steps[0],id:'3'}]}).valid).toBe(false));it('applies status, tier, and exclusion eligibility',()=>{expect(eligibleForPlaybook(playbook,90,'A')).toBe(true);expect(eligibleForPlaybook(playbook,90,'B')).toBe(false);expect(eligibleForPlaybook(playbook,90,'A',true)).toBe(false)});it('returns next ordered step',()=>expect(nextStep(playbook,1)?.id).toBe('2'));it('enrolls only eligible accounts',()=>expect(enrollAccounts(playbook,[{id:'a',name:'A',score:90,tier:'A'},{id:'b',name:'B',score:80,tier:'B'}])).toHaveLength(1));it('calculates safe conversion rates',()=>{expect(conversionRate(5,10)).toBe(50);expect(conversionRate(1,0)).toBe(0)})});
