import {getWorkItems,getExecutionSummary} from '@/lib/data/execution';
import {getWorkspaceContext} from '@/lib/workspace-context';
export default async function Page(){const {activeWorkspace}=await getWorkspaceContext();const items=await getWorkItems(activeWorkspace!.id);const s=await getExecutionSummary(activeWorkspace!.id);return <><div className="hero"><div><p className="eyebrow">Execute</p><h1>Work Queue</h1><p className="muted">Prioritized human and workflow tasks generated by campaigns, opportunities, research, and approvals.</p></div></div><div className="grid cards section"><div className="card"><span className="muted">Open</span><div className="metric">{s.openTasks}</div></div><div className="card"><span className="muted">Overdue</span><div className="metric">{s.overdueTasks}</div></div><div className="card"><span className="muted">Blocked</span><div className="metric">{s.blockedTasks}</div></div></div><div className="section table-scroll"><table className="table"><thead><tr><th>Work item</th><th>Account</th><th>Owner</th><th>Due</th><th>Priority</th><th>Status</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><strong>{x.title}</strong><span className="table-sub">{x.description}{x.blockedReason?` · Blocked: ${x.blockedReason}`:''}</span></td><td>{x.accountName??'—'}</td><td>{x.owner}</td><td>{x.dueAt?new Date(x.dueAt).toLocaleDateString():'—'}</td><td><span className={`status status-${x.priority==='critical'?'blocked':'active'}`}>{x.priority}</span></td><td><span className={`status status-${x.status}`}>{x.status.replace('_',' ')}</span></td></tr>)}</tbody></table></div></>}
