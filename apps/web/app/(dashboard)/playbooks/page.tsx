import Link from 'next/link';import {getWorkspaceContext} from '@/lib/workspace-context';import {getPlaybooks,getOffers} from '@/lib/data/gtm';
export default async function Page(){const {activeWorkspace}=await getWorkspaceContext();const [playbooks,offers]=await Promise.all([getPlaybooks(activeWorkspace!.id),getOffers(activeWorkspace!.id)]);return <><div className="hero"><div><div className="eyebrow">Execution design</div><h1>Playbooks</h1><p className="muted">Convert approved offers into governed, multichannel sales motions.</p></div><button className="btn button-reset">Create playbook</button></div><section className="section"><table className="table"><thead><tr><th>Playbook</th><th>Status</th><th>Offer</th><th>Audience</th><th>Steps</th><th></th></tr></thead><tbody>{playbooks.map(p=><tr key={p.id}><td><strong>{p.name}</strong><small className="table-sub">{p.trigger}</small></td><td><span className={`status status-${p.status}`}>{p.status}</span></td><td>{offers.find(o=>o.id===p.offerId)?.name}</td><td>Tier {p.targetTier}</td><td>{p.steps.length}</td><td><Link className="text-link" href={`/playbooks/${p.id}`}>Design →</Link></td></tr>)}</tbody></table></section></>}
