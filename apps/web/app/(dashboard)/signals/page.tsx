import Link from 'next/link';import {getWorkspaceContext} from '@/lib/workspace-context';import {getSignals} from '@/lib/data/research';
export default async function Page(){const {activeWorkspace}=await getWorkspaceContext();const signals=await getSignals(activeWorkspace!.id);return <><div className="hero"><div><div className="eyebrow">Intelligence</div><h1>Signals center</h1><p className="muted">Time-sensitive events and conditions that should trigger GTM action.</p></div><button className="secondary-btn">Configure signal types</button></div><section className="section"><table className="table"><thead><tr><th>Signal</th><th>Account</th><th>Strength</th><th>Confidence</th><th>Detected</th><th>Recommended action</th></tr></thead><tbody>{signals.map(s=><tr key={s.id}><td><strong>{s.signalType}</strong><small className="table-sub">{s.summary}</small></td><td><Link className="text-link" href={`/accounts/${s.entityId}`}>{s.entityName}</Link></td><td>{s.strength}</td><td>{s.confidence}%</td><td>{new Date(s.detectedAt).toLocaleDateString()}</td><td>{s.recommendedAction}</td></tr>)}</tbody></table></section></>}
