import { getComplianceData } from '@/lib/data/compliance';
import { obligationCoverage, policyAcknowledgementRate, policyReviewOverdue } from '@/lib/compliance-engine';
export default function Page(){const d=getComplianceData();const now=new Date('2026-07-18T23:00:00Z');return <><div className="hero"><div><div className="eyebrow">Compliance governance</div><h1>Policies & Obligations</h1><p className="muted">Maintain approved policy versions and prove how controls satisfy each obligation.</p></div></div><div className="section"><h2>Policy lifecycle</h2><div className="grid cards">{d.policies.map(p=><div className="card" key={p.id}><div className="eyebrow">{p.version} · {p.framework}</div><h3>{p.title}</h3><p className="muted">Owner: {p.owner}</p><p><span className={`status status-${policyReviewOverdue(p,now)?'warning':'healthy'}`}>{p.status}</span></p><small>{policyAcknowledgementRate(p)}% acknowledged · review {p.nextReviewAt}</small></div>)}</div></div><div className="section"><h2>Regulatory obligations</h2><div className="grid cards">{d.obligations.map(o=><div className="card" key={o.id}><div className="eyebrow">{o.framework} · {o.citation}</div><h3>{o.requirement}</h3><p className="muted">{o.jurisdiction} · Owner: {o.owner}</p><div className="metric">{obligationCoverage(o)}%</div><small>{o.controlIds.length} controls · {o.evidenceIds.length} evidence items · {o.status}</small></div>)}</div></div></>}
