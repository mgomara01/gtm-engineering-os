import {queueHealth} from '@/lib/activation-engine';import {getActivationData} from '@/lib/data/activation';export default function Page(){const {queues}=getActivationData();return <><div className="hero"><div><p className="eyebrow">Operations</p><h1>Worker queues</h1><p className="muted">Queue-backed execution with bounded retries and dead-letter isolation.</p></div></div><div className="card section"><table><thead><tr><th>Queue</th><th>Status</th><th>Pending</th><th>Running</th><th>Failed</th><th>Oldest</th><th>DLQ</th></tr></thead><tbody>{queues.map(q=><tr key={q.id}><td>{q.name}</td><td><span className={`status status-${queueHealth(q)}`}>{q.status}</span></td><td>{q.pending}</td><td>{q.running}</td><td>{q.failed}</td><td>{q.oldestAgeMinutes} min</td><td>{q.deadLetterEnabled?'enabled':'disabled'}</td></tr>)}</tbody></table></div></>}
