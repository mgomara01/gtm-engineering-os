import { getDeveloperPlatformData } from '@/lib/data/developer';
export default function Page(){const{clients,requests}=getDeveloperPlatformData();return <><div className="hero"><div><div className="eyebrow">Developer Platform</div><h1>API Activity</h1><p className="muted">Operational request telemetry without request bodies, credentials, or sensitive payload data.</p></div><span className="pill">Last 24 hours</span></div><div className="table-scroll section"><table className="table"><thead><tr><th>Time</th><th>Client</th><th>Request</th><th>Result</th><th>Latency</th><th>Request ID</th></tr></thead><tbody>{requests.map(r=><tr key={r.id}><td>{new Date(r.occurredAt).toLocaleString()}</td><td>{clients.find(c=>c.id===r.clientId)?.name??r.clientId}</td><td><strong>{r.method}</strong> <code>{r.path}</code></td><td><span className={`status status-${r.responseCode<300?'succeeded':r.responseCode===429?'warning':'failed'}`}>{r.responseCode}</span></td><td>{r.latencyMs} ms</td><td><code>{r.requestId}</code></td></tr>)}</tbody></table></div><div className="card section"><h2>Log policy</h2><p className="muted">Store request metadata, authorization decisions, rate-limit outcomes, and correlation identifiers. Redact authorization headers, secrets, personal data, and business payload bodies before persistence.</p></div></>}
