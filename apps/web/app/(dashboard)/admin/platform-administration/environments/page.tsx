import {getPlatformAdministrationData} from '@/lib/data/platform-administration';
export default function Page(){const d=getPlatformAdministrationData();return <><div className="hero"><div><div className="eyebrow">Environment governance</div><h1>Configuration & Promotion</h1><p className="muted">Release versions, configuration fingerprints, approvals, schedules, and production change risk.</p></div></div><div className="grid three-col section">{d.environments.map(e=><div className="card" key={e.id}><span className="muted">{e.name}</span><div className="metric">{e.version}</div><p><span className={`status status-${e.status==='healthy'?'healthy':'warning'}`}>{e.status}</span></p><small>{e.configurationHash}<br/>Last deployed {new Date(e.lastDeploymentAt).toLocaleString()}</small></div>)}</div><div className="card section"><table><thead><tr><th>Change</th><th>Environment</th><th>Category</th><th>Risk</th><th>Status</th><th>Approval</th></tr></thead><tbody>{d.changes.map(c=><tr key={c.id}><td><strong>{c.summary}</strong><br/><small>{c.id}</small></td><td>{c.environment}</td><td>{c.category}</td><td>{c.risk}</td><td>{c.status}</td><td>{c.approvedBy||'Pending'}</td></tr>)}</tbody></table></div></>}
