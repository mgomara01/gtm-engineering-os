import { getAiOperationsData } from '@/lib/data/ai-operations';
import { agentReadyForProduction, agentReviewOverdue } from '@/lib/ai-operations-engine';
export default function Page(){const{agents}=getAiOperationsData();const now=new Date('2026-07-18T23:00:00Z');return <><div className="hero"><div><div className="eyebrow">AI registry</div><h1>Autonomous Agents</h1><p className="muted">Control agent purpose, authority, tools, budget, model version, evaluations, and production eligibility.</p></div></div><div className="section card"><table><thead><tr><th>Agent</th><th>Owner</th><th>Risk</th><th>Model</th><th>Evaluation</th><th>Production</th></tr></thead><tbody>{agents.map(a=><tr key={a.id}><td><strong>{a.name}</strong><br/><small>{a.purpose}</small></td><td>{a.owner}</td><td>{a.risk}</td><td>{a.model} · v{a.version}</td><td>{a.evaluationScore}%{agentReviewOverdue(a,now)?' · overdue':''}</td><td><span className={`status status-${agentReadyForProduction(a,now)?'healthy':'warning'}`}>{agentReadyForProduction(a,now)?'ready':a.status}</span></td></tr>)}</tbody></table></div></>}
