import { getResilienceData } from '@/lib/data/resilience';
import { vendorAssessmentOverdue, vendorRiskScore } from '@/lib/resilience-engine';
export default function Page(){const {vendors}=getResilienceData();const now=new Date('2026-07-18T23:00:00Z');return <><div className="hero"><div><div className="eyebrow">Resilience operations</div><h1>Third-Party Risk</h1><p className="muted">Assess operational concentration, sensitive-data exposure, assurance evidence, and contractual safeguards.</p></div></div><div className="section card"><div className="table-wrap"><table><thead><tr><th>Provider</th><th>Service</th><th>Dependency</th><th>Data access</th><th>Residual risk</th><th>Score</th><th>Assessment</th></tr></thead><tbody>{vendors.map(v=><tr key={v.id}><td><strong>{v.name}</strong><br/><small>{v.owner}</small></td><td>{v.service}</td><td>{v.criticalDependency?'Critical':'Supporting'}</td><td>{v.dataAccess}</td><td>{v.residualRisk}</td><td>{vendorRiskScore(v)}</td><td><span className={`status status-${vendorAssessmentOverdue(v,now)?'warning':'healthy'}`}>{vendorAssessmentOverdue(v,now)?'overdue':'current'}</span></td></tr>)}</tbody></table></div></div></>}
