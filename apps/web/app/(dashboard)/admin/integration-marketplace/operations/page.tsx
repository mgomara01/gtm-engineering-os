import {getIntegrationMarketplaceData} from '@/lib/data/integration-marketplace';export default function Page(){const d=getIntegrationMarketplaceData();return <><div className="hero"><div><div className="eyebrow">Operations</div><h1>Connector Operations</h1><p className="muted">Installation health, credential lifecycle, synchronization throughput, and remediation.</p></div></div><div className="grid two-col section"><div className="card"><h2>Installations</h2>{d.installations.map(i=><div className="list-row" key={i.id}><div><strong>{i.id}</strong><br/><small>{i.owner} · {i.credentialType}</small></div><span className={`status status-${i.status==='healthy'?'healthy':'warning'}`}>{i.status}</span></div>)}</div><div className="card"><h2>Open alerts</h2>{d.alerts.map(a=><div className="list-row" key={a.id}><div><strong>{a.type}</strong><br/><small>{a.message}</small></div><span className="status status-warning">{a.severity}</span></div>)}</div></div><div className="section card"><h2>Recent sync jobs</h2><table><thead><tr><th>Job</th><th>Object</th><th>Status</th><th>Written</th><th>Failed</th><th>Retries</th></tr></thead><tbody>{d.jobs.map(j=><tr key={j.id}><td>{j.id}</td><td>{j.objectType}</td><td>{j.status}</td><td>{j.recordsWritten.toLocaleString()}</td><td>{j.recordsFailed}</td><td>{j.retryCount}</td></tr>)}</tbody></table></div></>}
