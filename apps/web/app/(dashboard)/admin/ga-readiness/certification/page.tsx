import {getGAReadinessData} from '@/lib/data/ga-readiness';import {acceptanceCoverage,blockingRisks,certificationCoverage} from '@/lib/ga-readiness-engine';
export default function Page(){const d=getGAReadinessData();return <><div className="hero"><div><div className="eyebrow">GA Certification</div><h1>Certification & Residual Risk</h1><p className="muted">Evidence-backed certification by control domain and cross-functional acceptance.</p></div></div><div className="grid cards section"><div className="card"><span className="muted">Certification</span><div className="metric">{certificationCoverage(d.certifications)}%</div></div><div className="card"><span className="muted">Acceptance</span><div className="metric">{acceptanceCoverage(d.acceptances)}%</div></div><div className="card"><span className="muted">Blocking risks</span><div className="metric">{blockingRisks(d.risks).length}</div></div></div><div className="card section"><h2>Certification domains</h2>{d.certifications.map(x=><div className="list-row" key={x.id}><div><strong>{x.domain}</strong><div className="muted">{x.owner} · {x.evidence}</div></div><span className={`status status-${x.status==='passed'?'healthy':'warning'}`}>{x.status}</span></div>)}</div><div className="card section"><h2>Functional acceptance</h2>{d.acceptances.map(x=><div className="list-row" key={x.id}><div><strong>{x.function}</strong><div className="muted">{x.owner} · {x.notes}</div></div><span className={`status status-${x.status==='accepted'?'healthy':'warning'}`}>{x.status}</span></div>)}</div></>}
