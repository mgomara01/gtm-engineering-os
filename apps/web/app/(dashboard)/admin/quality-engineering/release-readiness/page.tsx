import {getQualityEngineeringData} from '@/lib/data/quality-engineering';import {releaseGateFailures,uatApprovalRate} from '@/lib/quality-engineering-engine';
export default function Page(){const d=getQualityEngineeringData();const failures=releaseGateFailures(d.release);return <><div className="hero"><div><div className="eyebrow">Step 37</div><h1>Release Readiness</h1><p className="muted">A single command view for the production candidate, business acceptance, approvals, and rollback assurance.</p></div><span className={`status status-${failures.length?'warning':'healthy'}`}>{failures.length?`${failures.length} gates open`:'approved'}</span></div><div className="grid cards section"><div className="card"><span className="muted">Candidate</span><div className="metric">{d.release.version}</div><small>{d.release.status}</small></div><div className="card"><span className="muted">Required suites</span><div className="metric">{d.release.requiredSuitesPassed}/{d.release.requiredSuitesTotal}</div><small>passed</small></div><div className="card"><span className="muted">UAT</span><div className="metric">{uatApprovalRate(d.signoffs)}%</div><small>business approval</small></div><div className="card"><span className="muted">Rollback</span><div className="metric">{d.release.rollbackValidated?'Ready':'Open'}</div><small>production recovery path</small></div></div><div className="grid two-col section"><div className="card"><h2>Open release gates</h2>{failures.length?<ul>{failures.map(f=><li key={f}>{f}</li>)}</ul>:<p>No blocking gates.</p>}</div><div className="card"><h2>Approvals</h2><p>Security: {d.release.securityApproved?'approved':'pending'}</p><p>Performance: {d.release.performanceApproved?'approved':'pending'}</p><p>UAT: {d.release.uatApproved?'approved':'pending'}</p><p>Rollback: {d.release.rollbackValidated?'validated':'pending'}</p></div></div><div className="section card"><h2>Business sign-off</h2><div className="table-wrap"><table><thead><tr><th>Business area</th><th>Approver</th><th>Status</th><th>Notes</th></tr></thead><tbody>{d.signoffs.map(s=><tr key={s.id}><td>{s.businessArea}</td><td>{s.approver}</td><td>{s.status}</td><td>{s.notes}</td></tr>)}</tbody></table></div></div></>}
