import {getAgentRuns} from '@/lib/data/agents';import {getWorkspaceContext} from '@/lib/workspace-context';
export default async function Page(){const {activeWorkspace}=await getWorkspaceContext();if(!activeWorkspace)return null;const runs=await getAgentRuns(activeWorkspace.id);return <><p className="eyebrow">Observability</p><h1>Agent Run Monitor</h1><p className="muted">Trace model version, latency, token usage, cost, confidence, review requirements, and failures.</p><div className="card section table-scroll"><table className="table embedded"><thead><tr><th>Run</th><th>Agent / entity</th><th>Status</th><th>Version</th><th>Confidence</th><th>Latency</th><th>Cost</th><th>Review</th></tr></thead><tbody>{runs.map(r=><tr key={r.id}><td><code>{r.id}</code></td><td><strong>{r.agentName}</strong><span className="table-sub">{r.entityName}{r.error?` · ${r.error}`:''}</span></td><td><span className={`status status-${r.status}`}>{r.status.replace('_',' ')}</span></td><td>v{r.version}</td><td>{r.confidence||'—'}</td><td>{(r.latencyMs/1000).toFixed(1)}s</td><td>${r.cost.toFixed(2)}</td><td>{r.requiresReview?'Required':'No'}</td></tr>)}</tbody></table></div></>}
