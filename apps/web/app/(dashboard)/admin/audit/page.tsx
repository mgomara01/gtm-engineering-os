import {getGovernance} from '@/lib/data/governance';import {getWorkspaceContext} from '@/lib/workspace-context';
export default async function Page(){const {activeWorkspace}=await getWorkspaceContext();if(!activeWorkspace)return null;const {auditEvents}=await getGovernance(activeWorkspace.id);return <><div className="hero"><div><p className="eyebrow">Immutable evidence</p><h1>Audit log</h1><p className="muted">Actor, action, resource, correlation, severity, and timestamp lineage.</p></div></div><div className="table-scroll section"><table className="table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Resource</th><th>Summary</th><th>Severity</th><th>Correlation</th></tr></thead><tbody>{auditEvents.map(e=><tr key={e.id}><td>{new Date(e.occurredAt).toLocaleString()}</td><td>{e.actor}<small className="table-sub">{e.actorType}</small></td><td><code>{e.action}</code></td><td>{e.resourceType}<small className="table-sub">{e.resourceId}</small></td><td>{e.summary}</td><td><span className={`status alert-${e.severity}`}>{e.severity}</span></td><td><code>{e.correlationId}</code></td></tr>)}</tbody></table></div></>}
