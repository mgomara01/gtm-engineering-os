import {getWorkspaceContext} from '@/lib/workspace-context';import {getReviewQueue} from '@/lib/data/research';
export default async function Page(){const {activeWorkspace}=await getWorkspaceContext();const items=await getReviewQueue(activeWorkspace!.id);return <><div className="hero"><div><div className="eyebrow">Governance</div><h1>AI review inbox</h1><p className="muted">Review evidence-backed AI findings before they affect operational records or workflows.</p></div><div className="status-summary"><strong>{items.length}</strong><span>pending reviews</span></div></div><div className="review-stack section">{items.map(i=><article className="card" key={i.id}><div className="row-between"><div><div className="eyebrow">{i.type.replaceAll('_',' ')}</div><h3>{i.entityName}</h3></div><span className={`confidence ${i.confidence>=90?'confidence-high':'confidence-review'}`}>{i.confidence}%</span></div><p>{i.summary}</p><small className="muted">Created {new Date(i.createdAt).toLocaleString()}</small><div className="resolution-actions"><button className="btn button-reset">Review</button><button className="secondary-btn">Defer</button></div></article>)}</div></>}
