import{getRolloutData}from'@/lib/data/rollout';import{trainingSummary}from'@/lib/rollout-engine';export default function Page(){const d=getRolloutData(),s=trainingSummary(d.training);return <><div className="hero"><div><p className="eyebrow">Step 22 enablement</p><h1>Training certification</h1><p className="muted">Role-based completion, assessment, and go-live eligibility.</p></div><span className={`status status-${s.ready?'complete':'watch'}`}>{s.completionRate}% complete</span></div><div className="card section">{d.training.map(x=><div className="control-row" key={x.id}><span>{x.name}<small className="table-sub">{x.completed}/{x.assigned} complete · passing {x.passingScore}%</small></span><span className={`status status-${x.completed===x.assigned&&x.averageScore>=x.passingScore?'complete':'watch'}`}>{x.averageScore}% avg</span></div>)}</div></>}
