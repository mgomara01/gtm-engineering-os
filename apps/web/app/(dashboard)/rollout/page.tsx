import Link from'next/link';import{adoptionStatus,cohortProgress,migrationSummary,stabilizationDecision,trainingSummary,hypercareSummary}from'@/lib/rollout-engine';import{getRolloutData}from'@/lib/data/rollout';
export default function Page(){const d=getRolloutData(),m=migrationSummary(d.migration),t=trainingSummary(d.training),h=hypercareSummary(d.issues),ready=stabilizationDecision(d.gates,d.migration,d.training,d.issues);return <><div className="hero"><div><p className="eyebrow">Step 22 pilot rollout</p><h1>Rollout and stabilization</h1><p className="muted">Cohort onboarding, adoption, migration reconciliation, training certification, and hypercare exit control.</p></div><span className={`status status-${ready?'complete':'off_track'}`}>{ready?'ready to graduate':'stabilization blocked'}</span></div><div className="grid cards section"><div className="card"><span className="muted">Migration reconciliation</span><div className="metric">{m.reconciliationRate}%</div><small>{m.exceptions} exceptions</small></div><div className="card"><span className="muted">Training completion</span><div className="metric">{t.completionRate}%</div><small>{t.blockingIncomplete} blocking modules</small></div><div className="card"><span className="muted">Hypercare issues</span><div className="metric">{h.open}</div><small>{h.critical} critical · {h.high} high</small></div></div><div className="grid two-col section"><div className="card"><h3>Deployment cohorts</h3>{d.cohorts.map(c=>{const p=cohortProgress(c);return <div className="control-row" key={c.id}><span>{c.name}<small className="table-sub">{c.workspace} · {c.certified}/{c.users} certified · {c.accounts} accounts</small></span><span className={`status status-${p.live?'complete':'watch'}`}>{c.status}</span></div>})}</div><div className="card"><h3>Adoption telemetry</h3>{d.adoption.map(a=><div className="control-row" key={a.id}><span>{a.name}<small className="table-sub">Target {a.target}{a.unit==='percent'?'%':a.unit==='days'?' days':''}</small></span><span className={`status status-${adoptionStatus(a)==='on_track'?'complete':adoptionStatus(a)==='watch'?'watch':'off_track'}`}>{a.actual}{a.unit==='percent'?'%':a.unit==='days'?' days':''}</span></div>)}</div></div><div className="grid two-col section"><div className="card"><h3>Stabilization gates</h3>{d.gates.map(g=><div className="control-row" key={g.id}><span>{g.name}<small className="table-sub">{g.evidence}</small></span><span className={`status status-${g.status==='pass'?'complete':g.status==='warning'?'watch':'off_track'}`}>{g.status}</span></div>)}</div><div className="card"><h3>Operating workspaces</h3><p><Link href="/rollout/migration">Migration reconciliation</Link></p><p><Link href="/rollout/training">Training certification</Link></p><p><Link href="/rollout/hypercare">Hypercare operations</Link></p></div></div></>}
