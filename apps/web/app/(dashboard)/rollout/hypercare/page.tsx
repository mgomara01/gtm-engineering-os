import{getRolloutData}from'@/lib/data/rollout';import{hypercareSummary}from'@/lib/rollout-engine';export default function Page(){const d=getRolloutData(),s=hypercareSummary(d.issues);return <><div className="hero"><div><p className="eyebrow">Step 22 stabilization</p><h1>Hypercare operations</h1><p className="muted">Time-bound issue triage, ownership, mitigation, and graduation criteria.</p></div><span className={`status status-${s.ready?'complete':'off_track'}`}>{s.open} open</span></div><div className="card section">{d.issues.map(x=><div className="control-row" key={x.id}><span>{x.title}<small className="table-sub">{x.owner} · {x.ageHours} hours open</small></span><span className={`status status-${x.severity==='critical'||x.severity==='high'?'off_track':x.status==='resolved'?'complete':'watch'}`}>{x.severity} · {x.status}</span></div>)}</div></>}
