import {getApprovals} from '@/lib/data/execution';import {getWorkspaceContext} from '@/lib/workspace-context';
export default async function Page(){const {activeWorkspace}=await getWorkspaceContext();const rows=await getApprovals(activeWorkspace!.id);return <><p className="eyebrow">Govern</p><h1>Approval Center</h1><p className="muted">A consolidated queue for controlled changes and higher-risk GTM actions.</p><div className="section table-scroll"><table className="table"><thead><tr><th>Request</th><th>Type</th><th>Requested by</th><th>Reviewer</th><th>Age</th><th>Risk</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><strong>{x.objectName}</strong></td><td>{x.objectType}</td><td>{x.requestedBy}</td><td>{x.reviewerRole}</td><td>{x.ageHours}h</td><td><span className={`status status-${x.risk==='high'?'blocked':x.risk==='elevated'?'review':'active'}`}>{x.risk}</span></td><td><span className={`status status-${x.status==='pending'?'review':x.status}`}>{x.status.replace('_',' ')}</span></td></tr>)}</tbody></table></div><div className="notice section">Production decisions must execute through atomic server actions that verify reviewer permissions, preserve before/after snapshots, and write immutable audit events.</div></>}
