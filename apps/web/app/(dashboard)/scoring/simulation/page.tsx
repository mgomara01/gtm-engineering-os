import {getWorkspaceContext} from '@/lib/workspace-context';import {getScoringModels} from '@/lib/data/scoring';import {calculateScore} from '@/lib/scoring-engine';
export default async function Page(){const {activeWorkspace}=await getWorkspaceContext();const model=(await getScoringModels(activeWorkspace!.id))[0];const result=calculateScore(model,{factors:{fit:92,value:80,signal:75,relationship:65,confidence:90}});return <><div className="hero"><div><div className="eyebrow">Model testing</div><h1>Scoring simulation</h1><p className="muted">Test factor values before publishing a model version. Simulation never changes production scores.</p></div><button className="btn button-reset">Run simulation</button></div><div className="grid two-col section"><section className="card"><h2>Test inputs</h2>{model.factors.filter(f=>!f.hardExclusion).map((f,i)=><label key={f.id}>{f.name}<input className="input" type="number" defaultValue={[92,80,75,65,90][i]} min="0" max="100"/></label>)}<label><input type="checkbox"/> Trigger hard exclusion</label></section><section className="card simulation-result"><span className="muted">Simulated priority</span><div className="simulation-score">{result.score}</div><span className={`tier tier-${String(result.tier).toLowerCase()}`}>{result.tier}</span><p className="muted">Based on model v{model.version}. This result is not persisted.</p></section></div><section className="section"><h2>Factor explanation</h2><table className="table"><thead><tr><th>Factor</th><th>Raw</th><th>Weight</th><th>Contribution</th></tr></thead><tbody>{result.results.map(r=><tr key={r.factorId}><td>{r.factorName}</td><td>{r.rawScore}</td><td>{r.weight}%</td><td>{r.weightedScore}</td></tr>)}</tbody></table></section></>}
