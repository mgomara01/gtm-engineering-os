import { getExecutiveIntelligenceData } from '@/lib/data/executive-intelligence';
import { forecastSpreadPct } from '@/lib/executive-intelligence-engine';
const money=(n:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
export default function Page(){const d=getExecutiveIntelligenceData();return <><div className="hero"><div><div className="eyebrow">Executive Planning</div><h1>Forecasts & Scenarios</h1><p className="muted">Compare governed forward plans, assumptions, confidence, and operating outcomes.</p></div><span className="status status-healthy">{forecastSpreadPct(d.scenarios)}% spread</span></div><div className="grid three-col section">{d.scenarios.map(s=><div className="card" key={s.id}><div className="eyebrow">{s.confidence} confidence</div><h2>{s.name}</h2><div className="metric">{money(s.revenueUsd)}</div><p className="muted">{s.horizonMonths}-month revenue · {s.grossMarginPct}% gross margin · {s.pipelineCoverage}× coverage · {s.churnPct}% churn</p><h3>Assumptions</h3><ul>{s.assumptions.map(a=><li key={a}>{a}</li>)}</ul></div>)}</div><div className="card section"><h2>Governance standard</h2><p className="muted">Every scenario requires a named owner, dated assumptions, confidence rating, source-linked metrics, and a documented variance review before executive use.</p></div></>}
