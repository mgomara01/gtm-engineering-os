import {getWorkspaceContext} from '@/lib/workspace-context';import {getOperatingReview} from '@/lib/data/analytics';
export default async function Page(){const {activeWorkspace}=await getWorkspaceContext();const sections=await getOperatingReview(activeWorkspace!.id);return <><div className="hero"><div><div className="eyebrow">Weekly operating review</div><h1>Executive readout</h1><p className="muted">Answer-first review of performance, constraints, decisions, and accountable next actions.</p></div><button className="secondary-btn" disabled>Export review</button></div><div className="notice section">Demonstration review generated from controlled Step 16 data. Production exports require approved calculation runs and immutable review snapshots.</div><section className="section review-stack">{sections.map(s=><article className={`card review-section review-${s.status}`} key={s.title}><div className="row-between"><h2>{s.title}</h2><span className={`health health-${s.status}`}>{s.status}</span></div><p className="brief-summary">{s.summary}</p><div className="grid three-col"><div><h3>Key metrics</h3><ul>{s.metrics.map(x=><li key={x}>{x}</li>)}</ul></div><div><h3>Decisions required</h3><ul>{s.decisions.map(x=><li key={x}>{x}</li>)}</ul></div><div><h3>Next actions</h3><ul>{s.actions.map(x=><li key={x}>{x}</li>)}</ul></div></div></article>)}</section></>}
