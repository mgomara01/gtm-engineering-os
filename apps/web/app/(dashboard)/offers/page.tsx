import Link from 'next/link';import {getWorkspaceContext} from '@/lib/workspace-context';import {getOffers} from '@/lib/data/gtm';
export default async function Page(){const {activeWorkspace}=await getWorkspaceContext();const offers=await getOffers(activeWorkspace!.id);return <><div className="hero"><div><div className="eyebrow">Commercial packaging</div><h1>Offers</h1><p className="muted">Govern value propositions, eligibility, proof, pricing, objections, and approvals.</p></div><button className="btn button-reset">Create offer</button></div><div className="grid cards section"><div className="card"><span className="muted">Active</span><div className="metric">{offers.filter(o=>o.status==='active').length}</div></div><div className="card"><span className="muted">Approved</span><div className="metric">{offers.filter(o=>o.status==='approved').length}</div></div><div className="card"><span className="muted">Proof points</span><div className="metric">{offers.reduce((n,o)=>n+o.proofPoints.length,0)}</div></div></div><section className="section"><table className="table"><thead><tr><th>Offer</th><th>Status</th><th>Version</th><th>Target ICP</th><th>Pricing</th><th></th></tr></thead><tbody>{offers.map(o=><tr key={o.id}><td><strong>{o.name}</strong><small className="table-sub">{o.description}</small></td><td><span className={`status status-${o.status}`}>{o.status}</span></td><td>v{o.version}</td><td>{o.targetIcp}</td><td>{o.pricing}</td><td><Link className="text-link" href={`/offers/${o.id}`}>Open →</Link></td></tr>)}</tbody></table></section></>}
