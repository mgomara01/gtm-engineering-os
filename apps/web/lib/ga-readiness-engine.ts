import type {FunctionalAcceptance,GALaunchGate,MigrationReadiness,ReleaseCertification,ResidualRisk} from './ga-readiness-types';
export function certificationCoverage(items:ReleaseCertification[]){const required=items.filter(x=>x.required);if(!required.length)return 100;return Number((required.filter(x=>x.status==='passed'||x.status==='waived').length/required.length*100).toFixed(1));}
export function migrationReadinessScore(items:MigrationReadiness[]){if(!items.length)return 100;return Number((items.reduce((a,x)=>a+(x.status==='passed'?40:0)+(x.dryRunComplete?20:0)+(x.rollbackValidated?20:0)+(x.dataReconciled?20:0),0)/items.length).toFixed(1));}
export function blockingRisks(items:ResidualRisk[]){return items.filter(x=>x.status==='open'&&(x.level==='critical'||x.level==='high'));}
export function acceptanceCoverage(items:FunctionalAcceptance[]){if(!items.length)return 100;return Number((items.filter(x=>x.status==='accepted').length/items.length*100).toFixed(1));}
export function gaBlockers(items:GALaunchGate[]){return items.filter(x=>x.required&&x.status!=='ready');}
export function gaReadinessScore(certifications:ReleaseCertification[],migrations:MigrationReadiness[],risks:ResidualRisk[],acceptances:FunctionalAcceptance[],gates:GALaunchGate[]){const cert=certificationCoverage(certifications);const migration=migrationReadinessScore(migrations);const risk=Math.max(0,100-blockingRisks(risks).length*30);const acceptance=acceptanceCoverage(acceptances);const launch=Math.max(0,100-gaBlockers(gates).length*25);return Number(((cert+migration+risk+acceptance+launch)/5).toFixed(1));}
export function canCertifyGA(certifications:ReleaseCertification[],migrations:MigrationReadiness[],risks:ResidualRisk[],acceptances:FunctionalAcceptance[],gates:GALaunchGate[]){return certificationCoverage(certifications)===100&&migrationReadinessScore(migrations)===100&&blockingRisks(risks).length===0&&acceptanceCoverage(acceptances)===100&&gaBlockers(gates).length===0;}
